workflow_dispatch:name: Descargar repositorio
on:
  workflow_dispatch:
jobs:
  download-repository:
    runs-on: ubuntu-latest
    steps:
    - name: Check out repository
      uses: actions/checkout@v3
    - name: Ver contenido del repositorio
      run: |
        ls -al
